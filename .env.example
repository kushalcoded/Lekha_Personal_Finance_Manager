SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_publishable_key
# The Gemini key is NOT set here — it lives server-side as a secret of the
# gemini-proxy Edge Function (see SETUP_AUTH.md), so it never ships in the app.
